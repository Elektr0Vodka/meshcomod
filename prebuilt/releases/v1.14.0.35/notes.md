# v1.14.0.35

**Date:** 2026-03-22

- **V4 TFT + touch:** `heltec_v4_tft_companion_radio_usb_tcp_touch` — full meshcomod companion (multi-transport, WiFi inject, merged bin) for Heltec Wireless Stick Lite V3.1 with TFT + CHSC6x expansion; touch maps to the same short/long semantics as the physical USER button in `ui-new`.
- **Build flow:** `build.sh` produces `*-merged.bin` in `out/` for all `companion_radio_usb_tcp*` meshcomod envs automatically after each firmware build.
